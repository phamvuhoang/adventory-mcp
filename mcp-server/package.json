{
  "name": "adventory-mcp",
  "version": "0.1.0",
  "private": true,
  "description": "Read-only MCP server for an Adventory tenant's ads + inventory analytics",
  "main": "dist/stdio.js",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "start": "node dist/stdio.js",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "engines": { "node": ">=20" },
  "dependencies": { "@modelcontextprotocol/sdk": "^1.29.0" },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.5.0",
    "vitest": "^4.0.0"
  }
}
